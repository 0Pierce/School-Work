<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:A2_DynamoDB"
        xmlns:PdfViewer="clr-namespace:Syncfusion.Windows.PdfViewer;assembly=Syncfusion.PdfViewer.WPF" x:Class="A2_DynamoDB.EbookReader"
        mc:Ignorable="d"
        Title="EbookReader" Height="450" Width="800">
    <Grid>
        <Label x:Name="lblWelcome" Content="Hello " HorizontalAlignment="Left" Margin="20,10,0,0" VerticalAlignment="Top" FontSize="16" Height="35" Width="635"/>
        <ListBox x:Name="bookList" ItemsSource="{Binding Books}" Margin="0,50,0,0" SelectionChanged="bookList_Selected">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Background="AliceBlue" Orientation="Vertical">
                        <TextBlock Text="{Binding BookTitle}" FontWeight="Bold" FontSize="14" />
                        <TextBlock Text="{Binding BookAuthor}" FontStyle="Italic" FontSize="12" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

       

    </Grid>
</Window>
